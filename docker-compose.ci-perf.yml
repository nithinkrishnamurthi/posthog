# CI performance overlay — disable Postgres durability for ephemeral test data.
services:
    db:
        command: ["postgres", "-c", "fsync=off", "-c", "synchronous_commit=off", "-c", "full_page_writes=off"]
